
function createAutoComplete(arr){
	let names = arr;
	function autoComplete(inp){
		names = [];
		return names;
		
		
	}
return autoComplete;
}
var autoComplete = createAutoComplete();
autoComplete();